import { TestBed } from '@angular/core/testing';
import { DeletedModule } from './deleted.module';

describe('DeletedModule', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({ providers: [DeletedModule] });
  });
});
