import { UserActivatePipePipe } from './user-activate-pipe.pipe';

describe('UserActivatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new UserActivatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
